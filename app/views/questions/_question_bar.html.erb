<%= form_for @question, :html => {:class => "form-horizontal center"} do |f| %>
                      <div class="form-group">
                        <%= f.label :questions %>
                        <div class="col-md-5">
                          <%= f.text_field :body, class: "form-control", :placeholder => "hello" %>
                      <%= f.submit "Submit", class: "btn btn-default btn-primary" %>
                 <% end %>
                        </div>
                      </div>
